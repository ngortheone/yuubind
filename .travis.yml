language: rust
cache: cargo

sudo: required
os: [ linux ]

rust:
  - 1.26.0
  - stable
  - beta
  - nightly

matrix:
  include:
    - rust: stable
      env: TARPAULIN=1
    - rust: nightly
      env: CHECK_FORMAT=1

before_script: |
  export PATH="$HOME/.cargo/bin:$PATH"
  if [ -n "$TARPAULIN" ]; then
    cd ..
    git clone https://github.com/xd009642/tarpaulin
    cd tarpaulin
    cargo install --force
    # bash <(curl https://raw.githubusercontent.com/xd009642/tarpaulin/develop/travis-install.sh)
  elif [ -n "$CHECK_FORMAT" ]; then
    rustup component add rustfmt-preview --toolchain nightly
  fi

# TODO: (C) auto-build doc and push it to github pages (cargo doc and mdBook?)
script: |
  if [ -n "$TARPAULIN" ]; then
    cargo tarpaulin --out Xml
  elif [ -n "$CHECK_FORMAT" ]; then
    rustfmt --version
    cargo +nightly fmt --all -- --write-mode=check
  else
    cargo build --all --all-features
    cargo test --all --all-features
  fi

after_success: |
  if [ -n "$TARPAULIN" ]; then
    bash <(curl -s https://codecov.io/bash)
  fi
